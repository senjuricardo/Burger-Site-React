import React from "react";
import './Toolbar.css'
import Logo from "../../Logo/Logo";

const toolBar = (props) => (
<header className="Toolbar">
        <div>Menu </div>
        <Logo></Logo>
        <nav>
            <ul>..</ul>
        </nav>
</header>
);

export default toolBar